# Milestone 1 has no required runtime secrets; Open-Meteo needs no API key.
# This placeholder is reserved for the future server-side GPT workflow.
# Never expose it through a VITE_ variable or commit a real value.
OPENAI_API_KEY=
